# Release builds for the Notes Android app.
#
# Signed release APK/AAB in CI (optional): add repository secrets (never commit keys):
#   ANDROID_RELEASE_KEYSTORE_BASE64, ANDROID_RELEASE_STORE_PASSWORD,
#   ANDROID_RELEASE_KEY_ALIAS, ANDROID_RELEASE_KEY_PASSWORD
# The caller workflow must pass secrets (see ci.yml: secrets: inherit on the release job).
# materialize_release_signing.sh creates app/keystore.properties + app/ci-release-signing.jks
# which match app/build.gradle.kts when keystore.properties exists.

name: Release

on:
  workflow_call:

env:
  ANDROID_API_LEVEL: "37.0"
  ANDROID_BUILD_TOOLS: "37.0.0"
  JAVA_VERSION: "21"

jobs:
  release-assemble:
    name: Release Assemble
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Materialize release signing from GitHub secrets (optional)
        shell: bash
        env:
          ANDROID_RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
          ANDROID_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
          ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
        run: bash .github/scripts/release/materialize_release_signing.sh

      - name: Set up toolchain
        uses: ./.github/actions/setup-android-gradle
        with:
          java-version: ${{ env.JAVA_VERSION }}
          android-api-level: ${{ env.ANDROID_API_LEVEL }}
          android-build-tools: ${{ env.ANDROID_BUILD_TOOLS }}

      - name: Assemble release variants
        shell: bash
        run: bash .github/scripts/release/assemble_release.sh

      - name: Upload release build artifacts
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-build-reports
          if-no-files-found: ignore
          path: |
            **/build/outputs/apk/release/**
            **/build/outputs/bundle/release/**

  release-lint:
    name: Release Lint
    needs: release-assemble
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Materialize release signing from GitHub secrets (optional)
        shell: bash
        env:
          ANDROID_RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
          ANDROID_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
          ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
        run: bash .github/scripts/release/materialize_release_signing.sh

      - name: Set up toolchain
        uses: ./.github/actions/setup-android-gradle
        with:
          java-version: ${{ env.JAVA_VERSION }}
          android-api-level: ${{ env.ANDROID_API_LEVEL }}
          android-build-tools: ${{ env.ANDROID_BUILD_TOOLS }}

      - name: Lint release variants
        shell: bash
        run: bash .github/scripts/release/lint_release.sh

      - name: Upload release artifacts
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-lint-reports
          if-no-files-found: ignore
          path: |
            **/build/reports/lint-results-*.html
            **/build/reports/lint-results-*.xml
